# Canonical package state

Owner: sole Sol initiative leader acting as repository-artifact builder  
Ownership began: 2026-08-10 03:59 Asia/Singapore  
Status: **TERMINAL / VERIFIED / GITHUB-READY / LOCAL ONLY / NOT PUBLISHED**

This is the initiative's one canonical GitHub-ready artifact folder. There is
no competing repository tree or nested Git repository.

## Terminal evidence

- Outcome: `RANK_SPECIFIC_BREAK`.
- Prospective rank 4: valid `NONTRANSFER_OR_MIXED`; every substantive transfer
  gate failed.
- Conditionally earned matched rank 8: valid `TRANSFER_SUPPORTED`; every
  frozen gate passed on the same seeds.
- Both source classifications, the terminal integration, exact rank-4 and
  rank-8 margins, and a fixed 24-record representative sample are included and
  fingerprint-bound.
- All 24 train/eval receipts completed successfully; chronology, raw record
  counts, summaries, settings, and source/data/model fingerprints passed the
  frozen analyzer.
- A second implementation independently recomputed raw metrics, selections,
  aggregates, gates, chronology, commands, and all 126 evidence fingerprints;
  it passed with `errors: []`. This is same-owner implementation independence,
  not separate-owner outcome verification.
- The serialized local model slot was released after a fresh zero-process
  check; the portable receipt is `validation/model-slot-release.json`.
- A frozen model-free follow-up was independently recomputed and packaged as
  `POST-HOC-MECHANISM.md`. It describes the observed rank-associated selector
  and test-order reversals without changing `RANK_SPECIFIC_BREAK` or making a
  causal claim.

## Package contents and boundaries

- Complete frozen synthetic dataset and portable protocol/methods.
- Exact imported trainer/evaluator/capture sources plus prospective runner,
  analyzer, deterministic synthesizer, evidence exporters, and corruption
  fixtures.
- Historical rank-8 evidence retained only as context; matched rank 8 is the
  causal control for the rank-4 failure.
- Results, provenance, limitations, authorship, reproduction guidance, and a
  verification review that distinguishes pre-outcome separate-owner review
  from same-owner final-outcome inspection and independent implementation.
- No model weights, adapter weights, bulky transient logs, caches, secrets,
  private data, cloud dependency, external mutation, publication, or remote
  GitHub repository.

## Final gate

`MANIFEST.sha256` was generated only after every other file was fixed. The
documented model-free fixture suite passed, and `verify_package.py --stage
final` returned `PASS`, `errors: []`, and 60 files both in place and in an
isolated package copy. The package is therefore locally GitHub-ready; this
does not authorize publication or claim separate-owner post-outcome
verification.
